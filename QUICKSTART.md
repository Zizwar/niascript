# 🚀 البدء السريع - NiaScript 2.0

## 📦 التثبيت

```bash
npm install
```

---

## ⚡ البدء الفوري (بدون API)

النظام يعمل ممتاز **بدون API** للحسابات المحلية:

```bash
# أمثلة عملية مع حسابات حقيقية
npm run demo:local

# اختبارات شاملة
npm run test:ai

# عرض الفلسفة
npm run demo:simple
```

### ماذا يعمل بدون API؟

✅ **جميع الحسابات الرياضية**
```javascript
10 + 20 * 2 = 50
2^10 = 1024
(100 - 20) * 3 + 50 = 290
```

✅ **الفائدة المركبة**
```javascript
1000$ @ 8% لمدة 5 سنوات = $1,469.33
5000$ @ 12% لمدة 10 سنوات = $15,529.24
```

✅ **النسب المئوية**
```javascript
15% من 200 = 30
خصم 30% من 1250$ = $875
```

✅ **سيناريوهات عملية**
- حساب ROI
- مقارنة استثمارات
- حساب الخصومات
- تقييم الفرص المالية

---

## 🔑 للحصول على الذكاء الكامل (مع API)

### 1. احصل على مفتاح OpenRouter

1. اذهب إلى [openrouter.ai](https://openrouter.ai)
2. سجل حساب مجاني
3. اذهب إلى [Keys](https://openrouter.ai/keys)
4. أنشئ مفتاح جديد
5. انسخ المفتاح

### 2. أضف المفتاح

```bash
# انسخ ملف الإعدادات
cp .env.example .env

# عدّل .env وأضف مفتاحك
nano .env
```

في `.env`:
```bash
OPENROUTER_API_KEY=your-actual-key-here
```

### 3. جرّب!

```bash
# اختبار كامل مع AI
npm run demo:ai:env
```

---

## 💻 الاستخدام البرمجي

### بسيط جداً

```javascript
import { nia } from 'niascript';

// يعمل مع أو بدون API
const result = await nia`احسب 15% من 200`;
console.log(result); // "30"

// مع API - أسئلة ذكية
const answer = await nia`ما هو blockchain؟`;
console.log(answer);

// حسابات معقدة (بدون API)
const investment = await nia`استثمار 1000$ بنسبة 8% لمدة 5 سنوات`;
console.log(investment); // "$1,469.33"
```

### متقدم

```javascript
import { NiaAI } from 'niascript';

const ai = new NiaAI({
  apiKey: process.env.OPENROUTER_API_KEY,
  model: 'fast',  // أو balanced, smart, creative
  logLevel: 'debug'
});

// معالجة نية
const result = await ai.process('سؤالك هنا');

// استخدام الأدوات مباشرة (بدون API)
const calc = await ai.executeCalculate({
  expression: '1000 * (1.08)^5',
  type: 'compound_interest'
});
console.log(calc.result); // "1469.33"
```

---

## 🎯 الأوامر المتاحة

### بدون API (تعمل الآن)

```bash
npm run demo:local     # أمثلة عملية شاملة ⭐
npm run demo:simple    # عرض الفلسفة
npm run test:ai        # اختبارات أساسية
```

### مع API (بعد إضافة المفتاح)

```bash
npm run demo:ai        # مباشرة مع المفتاح من الكود
npm run demo:ai:env    # مع المفتاح من .env ⭐
```

### للتطوير

```bash
npm start              # النظام القديم (للمقارنة)
npm run dev            # مع nodemon
```

---

## 📚 التوثيق الكامل

- **README-NEW.md** - دليل شامل
- **CHANGELOG-2.0.md** - تفاصيل التحول
- **examples/** - أمثلة متعددة

---

## ❓ المشاكل الشائعة

### "Access denied" من OpenRouter

**الحل:**
1. تحقق من صحة المفتاح على [openrouter.ai/keys](https://openrouter.ai/keys)
2. تأكد من وجود Credits
3. تحقق من أن المفتاح لم ينتهِ
4. حاول إنشاء مفتاح جديد

**البديل:** استخدم النظام بدون API (يعمل ممتاز!)

### الحسابات لا تعمل

```bash
# تأكد من التثبيت
npm install

# جرب الاختبارات
npm run test:ai

# يجب أن ترى: ✅ جميع الاختبارات نجحت
```

---

## 🌟 الخلاصة

```javascript
// النظام يعمل الآن بطريقتين:

// 1. بدون API (الحسابات والسيناريوهات)
npm run demo:local  // ⭐ ابدأ من هنا!

// 2. مع API (الذكاء الكامل)
// أضف مفتاح OpenRouter في .env
npm run demo:ai:env
```

---

**🎯 NiaScript 2.0 - حيث النية هي الكود**

للمزيد: [README-NEW.md](README-NEW.md)
