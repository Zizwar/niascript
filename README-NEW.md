# 🎯 NiaScript 2.0 - البرمجة بالنية

> **التحول العميق: من Plugins التقليدية إلى الذكاء الخالص**

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Node](https://img.shields.io/badge/node-%3E%3D16.0.0-brightgreen.svg)](https://nodejs.org)

---

## 🌟 الفلسفة

```
النية هي كل شيء.
AI يفهم. AI يقرر. AI ينفذ.
لا قواعد صارمة. فقط احتمالات وذكاء.
```

### ما تغير؟

#### ❌ النظام القديم
```
Input → Intent Parser → Domain Classification → Plugin Selection → Plugin Execution → Output
```
**المشاكل:**
- يحتاج plugin مخصص لكل مجال
- قواعد صارمة للتصنيف
- ترجمة تقليدية بالبحث في dictionaries
- صعب التوسع والصيانة

#### ✅ النظام الجديد
```
Input → AI Understanding & Decision → Tool Execution (if needed) → Output
```
**المزايا:**
- لا حاجة لـ plugins مخصصة
- AI يقرر الأدوات المناسبة ديناميكياً
- ترجمة بالذكاء مباشرة
- سهل التوسع - من ذرة إلى غابة

---

## 🚀 البداية السريعة

### التثبيت

```bash
npm install niascript
```

### الاستخدام الأساسي

```javascript
import { nia } from 'niascript';

// سؤال بسيط
const answer = await nia`ما هو الذكاء الاصطناعي؟`;
console.log(answer);

// جلب بيانات
const btcPrice = await nia`سعر البيتكوين الحالي`;
console.log(btcPrice);

// حسابات مالية
const roi = await nia`إذا استثمرت 1000$ بنسبة 8% لمدة 5 سنوات؟`;
console.log(roi);

// ترجمة
const translation = await nia`ترجم "Hello World" للعربية`;
console.log(translation);
```

---

## 🎯 كيف يعمل؟

### الذكاء يقرر الأدوات

بدلاً من تحديد plugins مسبقاً، AI يختار الأدوات المناسبة:

```javascript
// المستخدم: "سعر البيتكوين"
// ↓
// AI يفكر: "يحتاج بيانات خارجية"
// ↓
// AI يستدعي: fetch_data({ url: "https://api.binance.com/..." })
// ↓
// النتيجة: "BTC: $42,580 USD"

// المستخدم: "احسب 15% من 200"
// ↓
// AI يفكر: "عملية حسابية"
// ↓
// AI يستدعي: calculate({ expression: "15% of 200", type: "percentage" })
// ↓
// النتيجة: "30"

// المستخدم: "ترجم Hello للعربية"
// ↓
// AI يفكر: "ترجمة بسيطة - من معرفتي"
// ↓
// النتيجة: "مرحبا"
```

### الأدوات المتاحة

| أداة | الوصف | مثال |
|------|--------|------|
| `fetch_data` | جلب بيانات من APIs | سعر العملات، بيانات الطقس |
| `calculate` | حسابات رياضية ومالية | فوائد مركبة، نسب مئوية |
| `search_web` | البحث في الإنترنت | أخبار، معلومات حديثة |

---

## 🧠 Models المتاحة

عبر **OpenRouter** - وصول لـ 400+ نموذج:

```javascript
import { NiaAI } from 'niascript';

// استخدام نموذج محدد
const nia = new NiaAI({
  apiKey: 'your-openrouter-key',
  model: 'fast'  // أو 'balanced' أو 'smart' أو 'creative'
});
```

### النماذج

| Model | الاسم | التكلفة | الاستخدام |
|-------|-------|---------|-----------|
| `fast` | Claude 3 Haiku | $0.25/1M tokens | مهام بسيطة وسريعة |
| `balanced` | GPT-4o-mini | $0.15/1M tokens | مهام متوسطة |
| `smart` | Deepseek Chat | $0.27/1M tokens | مهام معقدة |
| `creative` | Claude 3.5 Sonnet | $3/1M tokens | مهام إبداعية جداً |

### مقارنة Models

```javascript
// جرب نفس السؤال مع models مختلفة
const results = await nia.tryModels(
  'شرح blockchain في جملة واحدة',
  ['fast', 'balanced', 'smart']
);

results.forEach(r => {
  console.log(`${r.model}: ${r.data}`);
  console.log(`⏱️  ${r.duration}ms`);
});
```

---

## 📖 أمثلة متقدمة

### 1. سياق المحادثة

```javascript
// NiaAI يحفظ السياق تلقائياً
await nia`اسمي أحمد`;
await nia`عمري 25`;
await nia`ما اسمي وعمري؟`;
// → "اسمك أحمد وعمرك 25 سنة"
```

### 2. استخدام متقدم

```javascript
import { NiaAI } from 'niascript';

const ai = new NiaAI({
  apiKey: process.env.OPENROUTER_API_KEY,
  model: 'smart',
  logLevel: 'debug'
});

// معالجة نية
const result = await ai.process('احسب ROI لاستثمار 5000$ بنسبة 12% لمدة 10 سنوات');
console.log(result.data);
console.log('Tools used:', result.tools_used);

// سؤال مباشر
const answer = await ai.ask('ما هي أفضل ممارسات البرمجة؟');

// مسح السياق
ai.conversationContext = [];
```

### 3. تكامل في تطبيق

```javascript
import express from 'express';
import { nia } from 'niascript';

const app = express();

app.get('/api/query', async (req, res) => {
  try {
    const result = await nia`${req.query.q}`;
    res.json({ success: true, data: result });
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});

app.listen(3000);
```

---

## ⚙️ الإعداد

### متغيرات البيئة

```bash
# .env
OPENROUTER_API_KEY=sk-or-v1-...
NIA_LOG_LEVEL=info
NIA_DEFAULT_MODEL=fast
```

### Configuration

```javascript
import { nia } from 'niascript';

// إعداد عام
nia.config({
  apiKey: 'your-key',
  model: 'balanced',
  logLevel: 'debug'
});

// مسح السياق
nia.clearContext();
```

---

## 🎨 الفرق العملي

### مثال: الترجمة

#### ❌ النظام القديم

```javascript
// يحتاج plugin كامل مع dictionary
class TranslationPlugin {
  constructor() {
    this.dictionary = {
      'hello': 'مرحبا',
      'goodbye': 'وداعاً',
      // ... آلاف الكلمات
    };
  }

  translate(text, to) {
    // بحث في dictionary
    // قواعد معقدة للجمل
    // ...
  }
}
```

#### ✅ النظام الجديد

```javascript
// مباشرة - AI يفهم ويترجم
await nia`ترجم "How are you?" للعربية`;
// → "كيف حالك؟"

await nia`translate "مرحبا" to English`;
// → "Hello"
```

### مثال: حسابات مالية

#### ❌ النظام القديم

```javascript
// يحتاج plugin مع قواعد محددة
class FinancePlugin {
  calculateCompoundInterest(principal, rate, time) {
    // كود محدد لهذه الصيغة فقط
    return principal * Math.pow(1 + rate, time);
  }

  calculateROI(investment, returns) {
    // كود آخر لـ ROI
  }

  // ... عشرات الدوال
}
```

#### ✅ النظام الجديد

```javascript
// AI يفهم أي سؤال مالي
await nia`استثمار 1000$ بنسبة 8% لمدة 5 سنوات`;
// → "$1,469.33"

await nia`ما هو ROI لو استثمرت 5000 وربحت 7500؟`;
// → "ROI: 50%"

await nia`mortgage payment for $300k at 4.5% for 30 years`;
// → "Monthly payment: $1,520.06"
```

---

## 🌱 من ذرة إلى غابة

### المبدأ

```
نبدأ صغاراً. نختبر. نتعلم. ننمو.
لا هيكلة معقدة من البداية.
كل إضافة مبنية على نجاح السابقة.
```

### المراحل

1. **الذرة** ✅
   - Core AI engine
   - 3 أدوات أساسية
   - أمثلة بسيطة

2. **البذرة** 🌱
   - دعم models متعددة
   - سياق المحادثة
   - error handling محسّن

3. **الشجرة** 🌳 (قريباً)
   - أدوات متقدمة
   - memory system
   - caching ذكي

4. **الغابة** 🌲🌲🌲 (المستقبل)
   - multi-agent collaboration
   - plugin ecosystem جديد
   - self-improving AI

---

## 🧪 الاختبار

### تشغيل الأمثلة

```bash
# عرض الفلسفة
npm run demo:simple

# اختبار مع API (يحتاج OPENROUTER_API_KEY)
npm run demo:ai

# النظام القديم للمقارنة
npm run start
```

### Demos المتاحة

```bash
examples/
├── simple-demo.js      # عرض الفلسفة بدون API
├── ai-demo.js          # اختبار كامل مع OpenRouter
└── old-demo.js         # النظام القديم للمقارنة
```

---

## 📊 المقارنة

| الميزة | القديم | الجديد |
|--------|--------|--------|
| عدد الأسطر | ~2000 | ~600 |
| Plugins مطلوبة | نعم (لكل مجال) | لا |
| سهولة الإضافة | صعب | سهل جداً |
| الترجمة | Dictionary ثابت | AI ديناميكي |
| الحسابات | دوال محددة | فهم طبيعي |
| Models | OpenAI فقط | 400+ عبر OpenRouter |
| التكلفة | ثابتة | مرنة حسب الحاجة |

---

## 🚧 التطوير المستقبلي

### قريباً

- [ ] أدوات متقدمة (database, file system)
- [ ] Memory system ذكي
- [ ] Streaming responses
- [ ] Rate limiting محسّن
- [ ] Web interface

### الرؤية

```
نظام يتعلم من الاستخدام
يقترح تحسينات
ينمو مع المستخدم
يصبح أذكى مع الوقت
```

---

## 💡 لماذا هذا التحول؟

### الإلهام

> "أردت نظاماً يفهم نيتي مباشرة، لا نظاماً أخبره كيف يفهم."

### المشكلة

- **Plugins التقليدية**: كل حالة جديدة تحتاج plugin جديد
- **القواعد الصارمة**: لا مرونة في الفهم
- **الصيانة**: كل تحديث يؤثر على كل شيء

### الحل

- **AI المباشر**: يفهم أي نية
- **الاحتمالات**: يتكيف مع السياق
- **النمو الطبيعي**: يبدأ صغيراً وينمو

---

## 🤝 المساهمة

هذا مشروع في بداية ثورته. مساهماتك مهمة!

### كيف تساهم

1. Fork المشروع
2. أنشئ branch جديد (`git checkout -b feature/amazing`)
3. Commit تغييراتك (`git commit -m 'Add amazing feature'`)
4. Push للـ branch (`git push origin feature/amazing`)
5. افتح Pull Request

### الأولويات

- [ ] اختبارات أكثر
- [ ] دعم languages أخرى
- [ ] أمثلة متقدمة
- [ ] توثيق أفضل

---

## 📄 الترخيص

MIT License - استخدم بحرية، طوّر، شارك!

---

## 👨‍💻 المطور

**Brahim BIDI**

- GitHub: [@zizwar](https://github.com/zizwar)
- Project: [NiaScript](https://github.com/zizwar/niascript)

---

## 🙏 شكر خاص

- **OpenRouter** - الوصول لـ 400+ نموذج AI
- **Anthropic Claude** - ذكاء قوي
- **OpenAI** - رواد المجال
- **Deepseek** - بدائل قوية

---

## 🎯 الخلاصة

```javascript
// النظام القديم
const result = await complexPluginSystem.parse()
  .classify()
  .selectPlugin()
  .execute();

// النظام الجديد
const result = await nia`ما تريده`;

// بساطة. ذكاء. فعالية. 🚀
```

---

**NiaScript 2.0 - حيث النية هي الكود** 🎯
