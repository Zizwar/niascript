#!/usr/bin/env node
// مثال على استخدام NIA Template Literals
// الطريقة الثورية لكتابة النوايا في JavaScript!

import { nia } from './nia-template.js';

console.log(`
╔═══════════════════════════════════════════════════════════════╗
║  🎯 NiaScript Template Literals - أمثلة عملية                 ║
╚═══════════════════════════════════════════════════════════════╝
`);

async function main() {
  // ═══════════════════════════════════════════════════════════
  // مثال 1: نية بسيطة
  // ═══════════════════════════════════════════════════════════
  console.log('━'.repeat(60));
  console.log('📝 مثال 1: نية بسيطة\n');

  const greeting = await nia`قل مرحبا بالعربية بطريقة ودية`;
  console.log('النتيجة:', greeting.toString());
  console.log('المدة:', greeting.nia.duration, 'ms');

  // ═══════════════════════════════════════════════════════════
  // مثال 2: مع JSON Schema
  // ═══════════════════════════════════════════════════════════
  console.log('\n' + '━'.repeat(60));
  console.log('📝 مثال 2: مع JSON Schema\n');

  const colors = await nia`اعطني 5 ألوان جميلة للتصميم`.format('json', {
    colors: 'array'
  });
  console.log('الألوان:', colors);

  // ═══════════════════════════════════════════════════════════
  // مثال 3: تركيب المتغيرات
  // ═══════════════════════════════════════════════════════════
  console.log('\n' + '━'.repeat(60));
  console.log('📝 مثال 3: تركيب المتغيرات\n');

  const userName = 'أحمد';
  const userAge = 25;

  const story = await nia`اكتب قصة قصيرة جداً (سطرين) عن شخص اسمه ${userName} عمره ${userAge} سنة`;
  console.log('القصة:', story.toString());

  // ═══════════════════════════════════════════════════════════
  // مثال 4: سلسلة من النوايا المترابطة
  // ═══════════════════════════════════════════════════════════
  console.log('\n' + '━'.repeat(60));
  console.log('📝 مثال 4: سلسلة من النوايا المترابطة\n');

  // الخطوة 1
  const idea = await nia`اقترح فكرة تطبيق مبتكر في جملة واحدة`;
  console.log('1. الفكرة:', idea.toString());

  // الخطوة 2: استخدام الفكرة
  const features = await nia`اقترح 3 مميزات لهذا التطبيق: ${idea}`.json({
    features: 'array'
  });
  console.log('2. المميزات:', features);

  // الخطوة 3: استخدام كل شيء
  const pitch = await nia`اكتب pitch قصير (جملتين) لتطبيق فكرته ${idea} ومميزاته ${features}`;
  console.log('3. الـ Pitch:', pitch.toString());

  // ═══════════════════════════════════════════════════════════
  // مثال 5: الوصول للـ metadata
  // ═══════════════════════════════════════════════════════════
  console.log('\n' + '━'.repeat(60));
  console.log('📝 مثال 5: الـ Metadata\n');

  const result = await nia`ما هي عاصمة فرنسا؟`;
  console.log('الجواب:', result.value);
  console.log('النية الأصلية:', result.nia.intent);
  console.log('وقت التنفيذ:', result.nia.duration, 'ms');
  console.log('المصدر:', result.nia.raw?.source);

  // ═══════════════════════════════════════════════════════════
  // مثال 6: جلب بيانات حقيقية
  // ═══════════════════════════════════════════════════════════
  console.log('\n' + '━'.repeat(60));
  console.log('📝 مثال 6: جلب أخبار من Hacker News API\n');

  const news = await nia`اجلب 3 أخبار من Hacker News API المجاني (https://hacker-news.firebaseio.com/v0/topstories.json)`;
  console.log('الأخبار:', news.toString().substring(0, 300));

  if (news.nia.filepath) {
    console.log('الملف المولد:', news.nia.filepath);
  }

  console.log('\n' + '━'.repeat(60));
  console.log('✅ انتهت الأمثلة!');
}

main().catch(console.error);
